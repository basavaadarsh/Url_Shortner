<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirecting...</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        let countdown = 5;
        function updateTimer() {
            if (countdown > 0) {
                document.getElementById('timer').innerText = countdown;
                countdown--;
                setTimeout(updateTimer, 1000);
            } else {
                window.location.href = "<%= longUrl %>";
            }
        }
        window.onload = updateTimer;
    </script>
</head>
<body class="bg-gray-900 text-white flex flex-col justify-center items-center h-screen">
    <h1 class="text-3xl font-bold mb-4">Redirecting in <span id="timer">5</span> seconds...</h1>
    <p class="mb-6">If not redirected, <a href="<%= longUrl %>" class="text-blue-400 underline">click here</a>.</p>
    <div class="bg-gray-800 p-6 rounded-lg shadow-lg animate-fadeIn">
        <h2 class="text-xl font-semibold mb-2">Sponsored Advertisement</h2>
        <ins class="adsbygoogle" style="display:block"
            data-ad-client="YOUR_ADSENSE_CLIENT_ID"
            data-ad-slot="YOUR_AD_SLOT"
            data-ad-format="auto"></ins>
        <script>(adsbygoogle = window.adsbygoogle || []).push({});</script>
    </div>
</body>
</html>
